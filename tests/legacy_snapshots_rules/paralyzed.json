{
  "name": "paralyzed",
  "triggers": ["ATTACK_DECLARED"],
  "condition": "event.attacker == entity or event.defender == entity",
  "effects": [
    { "action": "Cancel", "when": "event.attacker == entity" },
    { "action": "GrantAdvantage", "when": "event.defender == entity" }
  ],
  "_note": "TODO: Auto-fail STR and DEX saving throws (pending save system). Any attack that hits within 5 ft is a critical hit (pending melee/ranged distinction and auto-crit logic). SPELL_CAST cancellation (pending spell cast cancellation support)."
}
