{
  "name": "invisible",
  "program": [
    {
      "block": "trigger",
      "event": "ATTACK_DECLARED",
      "when": "event.attacker == entity or event.defender == entity",
      "then": [
        { "block": "grant_advantage", "condition": "event.attacker == entity" },
        { "block": "grant_disadvantage", "condition": "event.defender == entity" }
      ]
    }
  ],
  "_note": "TODO: Treated as heavily obscured for hiding (pending stealth/hiding system)."
}
