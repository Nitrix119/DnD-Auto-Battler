{
  "name": "restrained",
  "triggers": ["ATTACK_DECLARED"],
  "condition": "event.attacker == entity or event.defender == entity",
  "effects": [
    { "action": "GrantDisadvantage", "when": "event.attacker == entity" },
    { "action": "GrantAdvantage", "when": "event.defender == entity" }
  ],
  "_note": "TODO: Speed becomes 0 and cannot benefit from any bonus to speed (pending movement system). Disadvantage on DEX saving throws (pending save system)."
}
