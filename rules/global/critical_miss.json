{
  "name": "critical_miss_on_nat_1",
  "program": [
    {
      "block": "trigger",
      "event": "ATTACK_ROLLED",
      "priority": 0,
      "when": "event.roll == 1",
      "then": [
        { "block": "force_critical", "outcome": "miss" }
      ]
    }
  ]
}
